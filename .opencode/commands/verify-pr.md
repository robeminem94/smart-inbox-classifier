---
description: Verify a GitHub PR with relevant tests and checks
agent: pr-orchestrator
---

Run the `verify-pr` workflow for `$ARGUMENTS` in this repository.

Immediate GitHub status comments are approved. Run relevant tests, lint, typecheck, or build commands. Do not edit files, commit, or push. Report results on the PR and ClickUp if linked.
