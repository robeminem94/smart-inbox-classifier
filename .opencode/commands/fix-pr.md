---
description: Fix a GitHub PR with branch, commit, push, and reporting
agent: pr-orchestrator
---

Run the `fix-pr` workflow for `$ARGUMENTS` in this repository.

Immediate GitHub status comments are approved. Branch creation, commits, and pushes are approved when needed. Use branch names like `agent/pr-<number>-fix` when creating a new branch. Keep changes minimal and tied to the PR objective. Update ClickUp if a linked task is found and the MCP tools are available.
